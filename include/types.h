/**
 * @file types.h
 * @brief Defines custom types used throughout the project
 */

#ifndef TYPES_H
#define TYPES_H

#include <constants.h>

typedef char Token[MAX_TOKEN_SIZE]; 
// A simple list of tokens, where a token is a string.
typedef Token Tokens[MAX_TOKENS];

#endif